server {
    listen 80;
    server_name _;
    root /usr/share/nginx/html;
    access_log off;

    location = / {
        return 302 /play/plukh/;
    }

    location /play/plukh/ {
        try_files $uri $uri/ =404;
    }
}
